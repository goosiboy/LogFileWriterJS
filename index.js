const Logger = require('./logfileWriter.js');

Logger.DEBUGLOG("This is debugLog test (DEBUG MODE)");
Logger.ERRORLOG("This was an erroLog test (DEBUG MODE)");
Logger.INFOLOG("This was infoLog test (DEBUG MODE)");
